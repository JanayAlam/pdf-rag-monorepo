{
  "name": "pdf-rag",
  "version": "1.0.0",
  "description": "PDF RAG system that ingests PDFs, embeds them into a vector database, and answers chat queries using retrieval-augmented generation.",
  "author": "Janay Alam <janayalam820@gmail.com>",
  "private": true,
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "dev:worker": "lerna run dev:worker --parallel",
    "build": "lerna run build",
    "start": "lerna run start",
    "start:worker": "lerna run start:worker",
    "lint": "lerna run lint"
  },
  "keywords": [],
  "license": "CC-BY-NC-4.0",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "lerna": "^9.0.7"
  }
}